#!/usr/bin/env node

// const checkVersion = require('@copoch/tesla-template-html5')
// const console = require('')
// const Command = require('../extra/command')
// const pkg = require('../package.json')

// const semver = require('semver')
// const debug = require('debug')('cli')

// debug('process.version', pkg.version)
// debug('pkg.engines.node', pkg.engines.node)

// if (!semver.satisfies(process.version, pkg.engines.node)) {
//   return console.error([`The Node version requirement is ${pkg.engines.node}`,
//   `but the current version is ${process.version}`].join(', '))
// }

// debug('process.argv', process.argv)

// const chalk = require('chalk')
// const sudoBlock = require('sudo-block')
// const cliList = require('cli-list')
// const pkg = require('../package.json')
// const gens = cliList(process.argv.slice(2))

// downgrade permissions
// sudoBlock('\n' + chalk.red('没必要使用 root 权限') + '\n')




